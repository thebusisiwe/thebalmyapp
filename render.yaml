services:
  - type: web
    name: thebalmyapp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
    envVars:
      - key: API_KEY
        sync: false
      - key: FLASK_DEBUG
        value: "0"
